# ============================================================
# AutoDev-Frontier — Dependencies
# ============================================================

# --- Phase 1: Core Agentic Loop ---
langgraph>=0.2.0             # The graph engine for stateful multi-agent workflows
langchain-core>=0.2.0        # Base abstractions (prompts, messages, runnables)
langchain-google-genai>=1.0.0 # LangChain connector for Google Gemini
google-generativeai>=0.7.0   # Underlying Gemini SDK
pydantic>=2.0.0              # Structured output schemas / data validation
python-dotenv>=1.0.0         # Load API keys from .env file

# --- Phase 2: RAG & Observability (install later) ---
# chromadb>=0.5.0            # Local vector database for RAG
# langchain-community>=0.2.0 # Document loaders, text splitters
# langsmith>=0.1.0           # LLMOps: visual tracing dashboard
